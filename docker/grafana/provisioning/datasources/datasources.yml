apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://cashier-prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: "5s"
    editable: true