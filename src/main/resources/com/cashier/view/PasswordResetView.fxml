<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.cashier.controller.PasswordResetController"
      spacing="20"
      styleClass="password-reset-dialog">

    <padding>
        <Insets top="25" right="25" bottom="25" left="25"/>
    </padding>

    <!-- 标题 -->
    <Label text="%password_reset.title"
           styleClass="dialog-title"/>

    <!-- 说明信息 -->
    <Label text="%password_reset.description"
           styleClass="dialog-description"
           wrapText="true"/>

    <!-- 表单 -->
    <VBox spacing="15">
        <!-- 用户名 -->
        <VBox spacing="8">
            <Label text="%password_reset.username" styleClass="form-label"/>
            <TextField fx:id="usernameField"
                       promptText="%password_reset.username_hint"
                       prefWidth="300"
                       styleClass="dialog-input"/>
        </VBox>

        <!-- 邮箱 -->
        <VBox spacing="8">
            <Label text="%password_reset.email" styleClass="form-label"/>
            <TextField fx:id="emailField"
                       promptText="%password_reset.email_hint"
                       prefWidth="300"
                       styleClass="dialog-input"/>
        </VBox>

        <!-- 错误提示 -->
        <Label fx:id="errorLabel"
               styleClass="error-label"
               visible="false"
               managed="false"/>

        <!-- 成功提示 -->
        <Label fx:id="successLabel"
               styleClass="success-label"
               visible="false"
               managed="false"/>
    </VBox>

    <!-- 按钮 -->
    <HBox spacing="10" alignment="CENTER_RIGHT" style="-fx-padding-top: 10;">
        <Button text="%password_reset.cancel"
                onAction="#handleCancel"
                prefWidth="100"
                prefHeight="35"
                styleClass="secondary-button"/>
        <Button fx:id="submitButton"
                text="%password_reset.submit"
                onAction="#handleSubmit"
                prefWidth="120"
                prefHeight="35"
                styleClass="primary-button"/>
    </HBox>

</VBox>
