# Database Configuration
# 安全提示：建议设置环境变量 CASHER_DB_PASSWORD 来存储数据库密码，避免明文存储
# Windows: set CASHER_DB_PASSWORD=YourPassword
# Linux/Mac: export CASHER_DB_PASSWORD=YourPassword
# 如果设置了环境变量，下面的 db.password 配置将被忽略

db.url=jdbc:mysql://localhost:3306/cashier_system?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
db.username=root
db.password=19949887
db.pool.size=10
db.connection.timeout=30000
db.idle.timeout=600000
db.max.lifetime=1800000
